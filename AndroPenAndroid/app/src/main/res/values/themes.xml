<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AndroPen" parent="Theme.AppCompat.DayNight.NoActionBar" />
    <style name="DarkModeButton" parent="Widget.AppCompat.Button">
        <!-- Button Background -->
        <item name="android:background">#333333</item> <!-- Dark gray -->
        <item name="android:textColor">#FFFFFF</item> <!-- White text -->
        <item name="android:padding">16dp</item> <!-- Adjust padding -->
        <item name="cornerRadius">8dp</item> <!-- Rounded corners -->
        <item name="android:elevation">4dp</item> <!-- Subtle shadow for depth -->
        <item name="android:textAllCaps">true</item> <!-- Uppercase text -->
    </style>
</resources>